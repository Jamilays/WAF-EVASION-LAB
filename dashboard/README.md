# Dashboard — [Phase 6]

Vite + React + TypeScript + Tailwind. Reads from the engine's FastAPI. Tabs: Live Run, Results (table + heatmap), Payload Explorer, Compare Runs. Served on `http://127.0.0.1:3000`.
