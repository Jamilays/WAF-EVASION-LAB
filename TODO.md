# TODO

Items that would improve the lab but take more than a quick session.

*(All items resolved as of the latest commit. See the session log for the
closure context — TODO #1 architecturally-out-of-scope, #2 shipped as
`RouteResult.waf_headers` + the Coraza rule-ID middleware, #3 shipped as
`scripts/with-nix-libs`.)*
